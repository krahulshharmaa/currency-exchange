version: 2.1

executors:
  android:
    docker:
      - image: circleci/android:api-30  # Use a CircleCI Android image that includes necessary tools
    working_directory: ~/repo
    environment:
      ANDROID_HOME: /opt/android-sdk
      PATH: /opt/android-sdk/cmdline-tools/latest/bin:$PATH

jobs:
  build:
    executor: android
    steps:
      # Step 1: Checkout code
      - checkout

      # Step 2: Install Android SDK if needed
      - run:
          name: Install Android SDK (if not already installed)
          command: |
            if [ ! -d "/opt/android-sdk" ]; then
              mkdir -p /opt/android-sdk/cmdline-tools
              curl -o sdk-tools.zip https://dl.google.com/android/repository/commandlinetools-linux-8512546_latest.zip
              unzip sdk-tools.zip -d /opt/android-sdk/cmdline-tools
              rm sdk-tools.zip
              mv /opt/android-sdk/cmdline-tools/cmdline-tools /opt/android-sdk/cmdline-tools/latest
              /opt/android-sdk/cmdline-tools/latest/bin/sdkmanager --sdk_root=/opt/android-sdk --install "platform-tools" "platforms;android-33" "build-tools;33.0.0"
            fi

      # Step 3: Make gradlew executable
      - run:
          name: Make gradlew executable
          command: chmod +x gradlew

      # Step 4: Restore Gradle dependencies
      - restore_cache:
          keys:
            - gradle-cache-{{ checksum "gradle/wrapper/gradle-wrapper.properties" }}
            - gradle-cache-

      # Step 5: Set up dependencies
      - run:
          name: Install Dependencies
          command: ./gradlew dependencies

      # Step 6: Save Gradle cache
      - save_cache:
          paths:
            - ~/.gradle/caches
          key: gradle-cache-{{ checksum "gradle/wrapper/gradle-wrapper.properties" }}

      # Step 7: Build APK
      - run:
          name: Build Release APK
          command: ./gradlew assembleRelease

      # Step 8: Store APK artifacts
      - store_artifacts:
          path: app/build/outputs/apk/release/
          destination: apk

workflows:
  version: 2
  build:
    jobs:
      - build
